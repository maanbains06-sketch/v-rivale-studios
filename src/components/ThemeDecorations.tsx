import { memo } from 'react';

// Theme decorations have been disabled - component returns null
const ThemeDecorations = memo(() => {
  return null;
});

ThemeDecorations.displayName = 'ThemeDecorations';

export default ThemeDecorations;
